## 1. 後端閘門（先修地雷）

- [x] 1.1 廣播 API 在 filters 未指定課程時一律拒絕，不寄出任何信，並回可讀的錯誤訊息；預覽（confirm=false）同樣拒絕，讓操作者在算人數階段就知道要先選課。滿足 "Cohort announcements are broadcast manually" 的單一課程前提。驗證：route 測試斷言「無 course 的請求回 400 且 sendBatchEmails 零呼叫」「有 course 的請求照常寄送」，`pnpm test` 通過。

## 2. 後臺課程篩選介面

- [ ] 2.1 後臺頁面提供課程選擇控制項，選定後只列出該課程的名單、時段分布只反映該課程，且 CSV 匯出回傳的正是當下列出的列。滿足 "The admin waitlist view supports segmentation" 的課程篩選要求。驗證：以管理者身分於正式站選課程，確認列表、人數與匯出三者一致。
- [x] 2.2 課程選項來自實際有候補資料的課程，而非硬編碼清單，選項顯示課程標題而非 slug（查無標題則退回 slug）。驗證：選項對照函式的單元測試（含查無標題的降級）。

## 3. 前端不提供無範圍的廣播

- [ ] 3.1 未選課程時，廣播面板停用送出並明說「請先選擇課程」，操作者不會在「全部」狀態下按到廣播。滿足 "The interface offers no unscoped dispatch"。驗證：正式站目視確認未選課程時面板呈現停用狀態與說明文字。

## 4. 驗證與上線

- [x] 4.1 全套測試與 lint 通過，既有的退訂排除、二次確認、通知時間回寫皆未受影響。驗證：`pnpm test` 全綠；`npx eslint` 對改動檔案零 error；`npx tsc --noEmit` 零新錯誤。
- [ ] 4.2 正式站確認地雷已拆：未選課程時無法廣播；選定課程後預覽人數只算該課程。驗證：以管理者身分於 /admin/waitlist 實地操作至「預覽收件人數」為止（不實際寄出，現有 5 筆為真實候補者）。
