## 1. Recur 商品拆分（前置，PR #15）

- [x] 1.1 每期一組獨立商品：第一期 b3dc06/u0rnbc 名稱改回 8/16 並關閉，第二期新建 tpl4a90/dckcqar。驗證：三方 ID 交叉比對（Recur 回傳＝courses-config＝recur-product-config），確保 webhook 走 course 分支而非靜默失敗。

## 2. 設定檔

- [x] 2.1 交付 `Cohort` 介面與五門課的 `cohorts` 陣列，實現決策「期別放在設定檔而非資料庫」：期別是課程設定不是交易資料。驗證：tsc 逼出五門課全部補齊，無一漏網。
- [x] 2.2 交付 courses-config.test.ts 的一致性守門：頂層 date 必須等於招生中那一期、商品不得跨期共用、至多一期招生中。驗證：實際重演 ef7188e（只改頂層 date）與商品跨期共用，兩者皆讓測試立刻紅。

## 3. 資料層

- [x] 3.1 交付 `cohort_key` 於 course_enrollments／assignments／course_guests，並依 recur_product_id 回填，實現決策「回填以 recur_product_id 為據」。驗證：回填前後五個內容指紋逐字相同；23 筆全歸第一期、第二期 0 人；未歸期 0 筆。
- [x] 3.2 交付 pending 唯一索引收斂到期別：(course_id, coalesce(cohort_key,''), email)。coalesce 是必要的，null 在唯一索引中彼此不相等會讓防競態失效。驗證：換索引前確認現有 pending 在新鍵組合下零重複。

## 4. 報名

- [x] 4.1 交付 register route 記下 cohort_key（本次唯一碰結帳的檔案，僅新增一個欄位值）。無招生中期別時報名照常完成、cohort_key 留空——收錢的路不能因為期別沒設好就斷掉。
- [x] 4.2 交付 409 守門收斂到期別：原本以 (course, email) 判斷，第一期學員報第二期會被擋且錯誤訊息誤導。

## 5. 資格與作業

- [x] 5.1 交付 findEligibleStudent 回傳 cohortKeys，兩張表都查再合併，不於付費命中時提早返回。代價是每位學員多一次查詢，換到回訓生不會被漏掉一期。
- [x] 5.2 交付作業、繳交、上傳、獎勵四處的跨期擋門：course 相同擋不住任何事，必須比對 cohort_key。
- [x] 5.3 交付通知收斂到該作業所屬期別：其他期的學員打不開，寄給他們是指向鎖住的門。

## 6. 介面

- [x] 6.1 交付學員列表頁以期別分組（單期時不顯示標題，回訓生才看得到分組）。
- [x] 6.2 交付老師端：課程頁以期別分組、作業表單期別下拉、來賓表單期別下拉、未指定期別的作業以紅色標出。

## 7. 收尾

- [x] 7.1 全套驗證：402 個測試通過、next build 成功、tsc 零新錯誤、範圍邊界零越界。
- [x] 7.2 正式站實測跨期隔離：第一期學員只看到第一期（第二期 0 次）、第二期學員只看到第二期（第一期 0 次）、第一期貼第二期作業 id 回 404、第二期開自己那份回 200。測試資料已清除。
