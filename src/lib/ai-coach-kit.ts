/**
 * AI 教練工坊單一事實來源。webhook fulfilment 與 payment/success 頁文案共用此組常數，
 * 避免時效／次數上限在兩處各自維護而漂移（比照 ars-bundles.ts／army-kit.ts 的設計）。
 */

export const AI_COACH_KIT_PRODUCT_NAME = "AI 教練工坊";

/** 下載連結有效期（小時）。 */
export const DOWNLOAD_TTL_HOURS = 72;

export const MAX_DOWNLOADS = 3;
